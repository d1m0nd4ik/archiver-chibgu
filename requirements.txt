# GUI
PySide6>=6.5.0,<7

# VK API
vk_api>=11.9.9,<12

# Загрузка медиа
requests>=2.31.0,<3
yt-dlp>=2024.8.0

# Изображения и иконки
Pillow>=10.0.0,<12

# Конфигурация
python-dotenv>=1.0.0,<2

# NLP (преподаватели в тексте, нормализация)
# NumPy 1.x — для Python до 3.13 (совместимость со старыми CPU без SSE4.2).
# NumPy 2.x — единственный вариант с готовыми wheel для Python 3.13+ (без компилятора).
numpy>=1.24.0,<2.0.0; python_version < "3.13"
numpy>=2.1.0,<3.0.0; python_version >= "3.13"
pymorphy3>=2.0.0,<3
pymorphy3-dicts-ru>=2.4.0
natasha>=1.6.0,<2

# Парсинг и экспорт
beautifulsoup4>=4.12.0,<5
openpyxl>=3.1.0,<4
python-docx>=1.1.0,<2

# извлечение cookies из браузера (если yt-dlp не смог сам)
browser-cookie3>=0.19.1

# тесты (опционально)
pytest>=7.0.0,<9
